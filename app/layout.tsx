import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ClerkProvider } from "@clerk/nextjs";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Vunalet - Harvesting the Future",
  description: "Connect directly with local farmers and access the freshest produce while supporting sustainable agriculture",
  keywords: ["agriculture", "farm-to-table", "sustainable farming", "fresh produce", "Nigeria"],
  authors: [{ name: "Vunalet Team" }],
  openGraph: {
    title: "Vunalet - Harvesting the Future",
    description: "Connect directly with local farmers and access the freshest produce",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        >
          {children}
        </body>
      </html>
    </ClerkProvider>
  );
}
